# --- Android (android/) — the Hotwire Native app IS tracked; only build
# output and machine-specific files are ignored. (The old Expo-era rule
# ignoring /android wholesale was removed on purpose.) ---
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.kotlin/
*.apk
*.aab
*.jks
*.keystore

# --- IDE ---
.idea/
*.iml

# --- OS ---
.DS_Store
Thumbs.db

# --- Legacy Expo app (frozen snapshot, kept for reference only) ---
legacy-expo/node_modules/
legacy-expo/.expo/
legacy-expo/.env
